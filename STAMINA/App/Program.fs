open System
open System.IO

[<EntryPoint>]
let main argv =
    // 1. Load binary

    let binaryFile = File.ReadAllBytes("mybinary.txt")

    0

    
    // 2. Convert to image
    
    
    // 3. Use transfer learning to train a model to classify images
    
    
    // 4. Evaluate model
